# GSC Audit — tinypdftools.com

_Report generated: 2026-04-28_

This is the scheduled weekly GSC audit + auto-fix run.

## Run summary

- **Fresh GSC pull:** attempted, hung. `python3 scripts/gsc_audit.py` was invoked twice. Both runs printed `[gsc-audit] testing site URL: https://tinypdftools.com/` then `got 1 row(s)` and stalled in the `urlInspection().index().inspect()` loop. The same step timed out on the 2026-04-22 report (`Coverage / indexing — _Unexpected error: The read operation timed out_`), so this is a recurring upstream issue, not a one-off. The script catches `HttpError` but not `socket.timeout` / `URLError`, so a stuck request hangs indefinitely.
- **Fallback snapshot used:** `reports/gsc-audit-2026-04-22.md` (6 days old) for performance + Core Web Vitals; `reports/gsc-audit-2026-04-19.md` for URL-inspection coverage data (the most recent run that completed the urlInspection step successfully).

## Triage by bucket

### LOW CTR (>200 impressions, <1% CTR)

**0 fixes.** The 2026-04-22 snapshot has 14 total impressions sitewide across the entire 7-day window, and the only page reporting impressions is `/` (14 imps). No page comes close to the 200-impression threshold, so the LOW-CTR rewrite rule does not trigger for any blog post.

### INDEXING issues on blog pages

**0 fixes.** The 2026-04-19 snapshot inspected 18 URLs and reported 17 as not indexed:

- 11 in state `Discovered – currently not indexed`: `/merge-pdf`, `/split-pdf`, `/compress-pdf`, `/pdf-to-image`, `/image-to-pdf`, `/rotate-pdf`, `/add-page-numbers`, `/crop-pdf`, `/delete-pdf-pages`, `/flatten-pdf`, `/extract-pdf-pages`.
- 6 in state `URL is unknown to Google`: `/protect-pdf`, `/unlock-pdf`, `/watermark-pdf`, `/organize-pdf`, `/sign-pdf`, `/pdf-reader`.

Every unindexed URL is a **tool route**, not a `/blog/<slug>` route. The task spec scopes INDEXING content edits to blog pages only (`Check the JSON exists and content is substantive…`), so no blog JSON edits are warranted. Tool-route indexing is being addressed via IndexNow + GSC manual indexing requests per the 2026-04-19 follow-up notes, not via JSON post edits.

### Broken links

**0 fixes.** The GSC snapshot does not include sitewide crawl error data this week. As a sanity sweep I grepped all 57 blog-post JSON files for internal `/blog/<slug>` references and verified each target exists:

```
src/data/blogPosts/ — 15 unique internal /blog/* references — all targets resolve to live JSON files.
```

No internal-link 404s to repair. External-link rot is not detectable from the snapshot, and the spec only triggers fixes off the GSC data.

## Manual review queue

None this run.

## Verification sample

Skipped — no URLs touched, so nothing to re-fetch post-deploy.

## Notes for next run

- The audit script needs a per-request timeout on `urlInspection().index().inspect()` (currently relies on default socket timeout). Adding `socket.setdefaulttimeout(30)` near the top of `scripts/gsc_audit.py` would cause hung requests to raise instead of stalling the entire run, and the `except HttpError` block could be widened to `except (HttpError, OSError)` to keep the loop moving when one URL inspection times out.
- Working tree at run time had ~38 unrelated modified files (from in-flight component work) plus untracked scratch files. This run committed **only `reports/`** — staging was scoped explicitly to avoid bundling unrelated WIP into the audit commit.

---

_Hard-rules check: no Supabase calls; no JSON `slug`/`id`/`category`/`date`/URL changes; no posts deleted; no force-push; ≤10 fixes (0 applied); fallback snapshot used because fresh pull hung; no aggressive retry after the hang._
