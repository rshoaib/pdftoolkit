# GSC Audit — tinypdftools.com

_Report generated: 2026-05-05_

This is the scheduled weekly GSC audit + auto-fix run.

## Run summary

- **Fresh GSC pull:** attempted, hung again. `python3 scripts/gsc_audit.py` printed `[gsc-audit] testing site URL: https://tinypdftools.com/` and stalled in the `urlInspection().index().inspect()` loop, the same recurring failure mode noted in the 2026-04-22 and 2026-04-28 reports. Per the spec's "do not retry aggressively" rule, the script was killed after a bounded timeout instead of being retried again.
- **Fallback snapshot used:** `reports/gsc-audit-2026-04-22.md` (most recent run with a complete performance section); `reports/gsc-audit-2026-04-19.md` for the URL-inspection coverage data (most recent run that finished the urlInspection step).
- **Repo discipline:** the working tree had ~38 unrelated modified files (in-flight component work) plus several untracked scratch files. Staging was scoped explicitly to the two touched JSON files plus this report — no unrelated WIP was bundled into the commit.

## Triage by bucket

### LOW CTR (>200 impressions, <1% CTR)

**0 fixes.** The 2026-04-22 fallback snapshot reports 14 total impressions sitewide across the 7-day window, all attributed to `/`. No blog page registers any impressions, so nothing comes within two orders of magnitude of the 200-impression threshold. The LOW-CTR title/excerpt rewrite rule does not trigger.

### INDEXING issues on blog pages

**0 fixes.** The 2026-04-19 inspection covered 18 URLs and reported 17 unindexed — but every unindexed URL is a **tool route** (`/merge-pdf`, `/split-pdf`, …, `/pdf-reader`), not a `/blog/<slug>` route. The spec scopes INDEXING content edits to blog pages only, so no blog JSON edits are warranted. Tool-route indexing remains a crawl-budget issue being addressed via IndexNow + manual GSC indexing requests per the 2026-04-19 follow-up notes, not via JSON post edits.

### Broken links

**5 fixes (6 occurrences across 2 files).** Sitewide crawl error data was again absent from the GSC snapshot, so I did the spec-mandated grep sweep against the live blog corpus. Found 6 internal `[text](/blog/<slug>)` markdown links pointing at slugs that do not exist as JSON files (real 404s on the live site, not GSC-reported). Each was repointed to the closest live equivalent (target `.json` verified before write):

| Broken `/blog/` slug | Repointed to | Files touched |
| --- | --- | --- |
| `/blog/how-to-add-page-numbers-to-pdf` | `/blog/how-to-add-page-numbers-to-pdf-free` | `best-free-pdf-tools-online.json` |
| `/blog/how-to-compress-pdf-free-online` | `/blog/compress-pdf-without-losing-quality` | `best-free-pdf-tools-online.json`, `how-to-make-pdf-smaller-for-email.json` |
| `/blog/how-to-delete-pdf-pages-online-free` | `/blog/how-to-delete-pages-from-pdf-free` | `best-free-pdf-tools-online.json` |
| `/blog/how-to-unlock-pdf-free-online` | `/blog/how-to-unlock-pdf-remove-password-free` | `best-free-pdf-tools-online.json` |
| `/blog/pdf-to-image-converter-free` | `/blog/pdf-to-image-convert-pdf-to-png-jpg` | `best-free-pdf-tools-online.json` |

Anchor text preserved in every case (anchor sits inside `content`, which the spec forbids restructuring). Post-fix grep over `src/data/blogPosts/` confirms 0 remaining broken markdown `/blog/` links across all 59 posts; 24 internal `/blog/` markdown links now resolve cleanly.

## Manual review queue

- **Near-duplicate slug pair:** `how-to-fix-a-rotated-pdf-scan.json` and `how-to-fix-rotated-pdf-scan.json` cover the same primary keyword and topic. Per the hard rule "NEVER delete a post during this task", flagged for manual consolidation rather than automatic deletion.
- **Duplicate-stem candidate:** `best-free-pdf-tools-online.json` and `best-free-pdf-tools-online-2026.json` share the same primary keyword. Same rule — flagged for manual review.
- **GSC audit script reliability:** the urlInspection hang has now occurred on three consecutive runs (04-22, 04-28, 05-05). Recommended fix from the 04-28 report still applies: add `socket.setdefaulttimeout(30)` near the top of `scripts/gsc_audit.py` and widen the `except HttpError` block to `except (HttpError, OSError)` so a stuck inspection raises instead of stalling the entire run.

## Verification sample

Will be appended after the post-deploy WebFetch check (~2 minutes after push). Touched URLs to verify:

1. `https://tinypdftools.com/blog/best-free-pdf-tools-online`
2. `https://tinypdftools.com/blog/how-to-make-pdf-smaller-for-email`

Verification target is the rewritten internal links (the new `/blog/<slug>` `href`s should appear in the rendered post body). Title and meta description were not changed on either post — they're untouched by this run.

## Notes for next run

- Push status is recorded in the commit log below the report. The sandbox has no GitHub credentials by default, so any push from this environment will likely fail (per the 04-28 report). The audit commit is local; the user's machine will push it on next sync.
- All 6 link replacements together remain well under the 10-fix-per-run cap — 4 slots remained unused at the end of triage.

---

_Hard-rules check: no Supabase calls; no JSON `slug`/`id`/`category`/`date`/`displayDate`/`relatedToolLink`/`relatedToolName` field changes (only `content` body strings were edited inside markdown link targets, which the BROKEN-LINKS rule explicitly authorizes); no posts deleted; no force-push; ≤10 fixes (6 applied); fallback snapshot used because fresh pull hung; no aggressive retry after the hang._
